//
// Created by Momo on 2024/10/3.
//

#ifndef STDC_H
#define STDC_H

#ifndef MY_STDCXX_HEADER_H
#define MY_STDCXX_HEADER_H

// C++ 标准库头文件
#include <iostream>   // 输入输出
#include <vector>     // 动态数组容器
#include <string>     // 字符串类
#include <algorithm>  // 常用算法，如 sort、find 等
#include <functional> // 函数对象、bind 等
#include <memory>     // 智能指针，如 unique_ptr, shared_ptr
#include <mutex>      // 多线程互斥锁
#include <thread>     // 线程支持
#include <future>     // 异步任务及 futures
#include <atomic>     // 原子操作
#include <map>        // 有序映射容器
#include <set>        // 有序集合容器
#include <unordered_map> // 无序映射容器
#include <unordered_set> // 无序集合容器
#include <fstream>    // 文件流
#include <sstream>    // 字符串流
#include <utility>

// 如果需要数学函数
#include <cmath>      // 数学库

// 如果需要时间处理
#include <chrono>     // 处理时间的库

#endif // MY_STDCXX_HEADER_H

#endif //STDC_H
